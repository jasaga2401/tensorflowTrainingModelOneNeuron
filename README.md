# tensorflowTrainingModelOneNeuron
Developing a model using Tensorflow to train an AI with an input / output, so that the AI tries to work out the relationship between a series of inputs and attempts to predict the output based on this training. 
